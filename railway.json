{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bin/rails db:prepare && bin/rails server -b :: -p $PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}